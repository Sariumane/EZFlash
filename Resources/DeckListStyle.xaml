<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="DeckListStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0,0,0,8"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="ItemBorder"
                            Background="{StaticResource DeckCoverColor}"
                            Padding="16"
                            SnapsToDevicePixels="True"
                            CornerRadius="6"
                            Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}">
						<Border.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.Tag,
                                          RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Learn Scheduled"
                                          Command="{Binding LearnScheduledCommand}">
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/learn-scheduled.png"
                                               Width="14"
                                               Height="14"
                                               Stretch="Uniform"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem Header="Learn Free"
                                          Command="{Binding LearnFreeCommand}" >
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/learn-free.png"
                                               Width="14"
                                               Height="14"
                                               Stretch="Uniform"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <Separator/>

                                <MenuItem Header="Rename"
                                          Command="{Binding RenameDeckCommand}" >
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/rename.png"
                                               Width="14"
                                               Height="14"
                                               Stretch="Uniform"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem Header="Edit"
                                          Command="{Binding EditDeckCommand}" >
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/edit.png"
                                               Width="14"
                                               Height="14"
                                               Stretch="Uniform"/>
                                    </MenuItem.Icon>
                                </MenuItem>


                                <MenuItem Header="Delete" 
                                          Command="{Binding DeleteDeckCommand}"
                                          Style="{StaticResource DangerContextMenuItemStyle}" >
                                    <MenuItem.Icon>
                                        <Image Source="/Assets/delete.png"
                                               Width="14"
                                               Height="14"
                                               Stretch="Uniform"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                            </ContextMenu>
                        </Border.ContextMenu>

                        <ContentPresenter />
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ItemBorder"
                            Property="Opacity"
                            Value="0.85"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ItemBorder"
                            Property="BorderBrush"
                            Value="{StaticResource PrimaryTextBrush}"/>
                            <Setter TargetName="ItemBorder"
                            Property="BorderThickness"
                            Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>